#import "./userFields.graphql"

mutation currentUserForgotPasswordUpdate($input: UsersForgotPasswordUpdateInput!) {
  currentUserForgotPasswordUpdate(input: $input) {
    user {
      ...userFields
    },
    errors
  }
}
