query chat($id: Int!) {
  chat(id: $id) {
    id
    messages {
      id
      text
      sender {
        id
      }
    }
    companion {
      id
      firstName
      lastName
      thumbnailAvatar {
        url
      }
    }
  }
}
