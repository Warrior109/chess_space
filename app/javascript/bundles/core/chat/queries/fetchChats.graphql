#import "./chatListItemFields.graphql"

query chats($cursor: String) {
  chats(first: 15, after: $cursor) {
    pageInfo {
      endCursor
      hasNextPage
    }
    nodes {
      ...chatListItemFields
    }
  }
}
