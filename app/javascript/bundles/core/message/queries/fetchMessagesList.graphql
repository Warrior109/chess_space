#import "./messageFields.graphql"

query messages($cursor: String, $chatId: Int!) {
  messages(last: 15, before: $cursor, chatId: $chatId) {
    pageInfo {
      startCursor
      hasPreviousPage
    }
    nodes {
      ...messageFields
    }
  }
}
